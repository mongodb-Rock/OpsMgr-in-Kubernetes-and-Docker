# Deployment Options
tls=1 # yes (0 = no)

# DBs to expose externally
exposed_dbs=("my-replica-set") # set of DBs to expose
#horizon="LoadBalancer" # splitHorizon mode for external acess - default is NodePort
horizon="NodePort"

# OpsManager Admin
password="Mongodb1$"
firstName="Thomas"
lastName="Luckenbach"
user="thomas.luckenbach@mongodb.com"

# DB Admin
dbadmin="dbAdmin"
dbpassword="Mongodb1$"

# Update from Ops Manager after deployment of opsmgr to put K8s Deployed Projects into this Org.

opsMgrUrl="https://opsmanager-svc.mongodb.svc.cluster.local:8443"
opsMgrIp="10.1.0.173"
opsMgrUrlExt="https://localhost:8443"
myreplicaset_URI="mongodb://localhost:31000,localhost:31001,localhost:31002/?replicaSet=my-replica-set -u $dbadmin -p $dbpassword --authenticationDatabase admin " 
